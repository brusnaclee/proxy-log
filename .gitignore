# Dependencies
node_modules/

# Build outputs
dist/

# Environment / secrets — NEVER commit
.env

# SQLite database files
*.db
*.db-shm
*.db-wal
*.db-journal

# Runtime data directories
data/
packages/proxy/data/
packages/bot/data/

# Generated cache files
packages/proxy/data/model_catalog_cache.json

# TypeScript build cache
*.tsbuildinfo

# Tool caches
.turbo
.vite

# PM2 / process logs
packages/bot/logs/
*.log

# OS / editor artifacts
.DS_Store
Thumbs.db

# Temporary scripts and output files
*.py
b64.txt
ssh_output.txt
commit-msg.txt
all_commits.txt
server-deploy-output.txt
test_ps.txt
test_node.txt
agent-tools/

# Ad-hoc audit / debug captures (NEVER commit — may contain API keys / IPs / UAs)
audit-*.txt
deploy-out.txt
live-out.txt
restart-out.txt
pm2-err.txt
pm2-tail.txt
tsc-out.txt
.commit-msg.txt

# Local-only ops scripts (keep on disk, never on GitHub)
scripts/
packages/**/scripts/
**/scripts/

# Local-only ad-hoc verify / bootstrap scripts (must NOT be committed)
_*.mjs
**/_*.mjs

# Documentation & editor (local only)
docs/
.cursor/

# Packaging leftovers
proxy-src.tar.gz
