# Groupy Compact

Stubs older noisy tool dumps in long agent histories; keeps the last N tool results full.

Never touches write/edit tool results. Levels: lite / balanced / aggressive, or custom `keepLastN` + `stubMinChars`.

Part of **Groupy Token Saver** (default ON). See [token_saver.md](./token_saver.md).
